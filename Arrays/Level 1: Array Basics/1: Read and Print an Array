#include <stdio.h>

int main()
{
    int n,i;
    scanf("%d",&n);
    int arr[n];
    printf("Elements:%d\n",n);
    for(i=0;i<=n;i++){
        scanf("%d",&arr[i]);
        
    }printf("Array Elements are:\n");
    for(i=0;i<=n;i++){
        printf("%d\n",arr[i]);
    
}
    return 0;
}
