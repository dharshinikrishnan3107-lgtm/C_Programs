#include <stdio.h>

int main()
{
    int Area,Length,Breadth;
    scanf("%d", &Length);
    scanf("%d", &Breadth);
    Area = Length*Breadth;
    printf("Area of Rectanglele:%d\n",Area);
     
    return 0;
}
