#include <stdio.h>

int main()
{
    char ch;
    scanf("%c",&ch);
    if (ch>='A' && ch<='Z'|| ch>='a' && ch<='z'){
        printf("Character");
    }else if(ch>='0' && ch<='9'){
        printf("Digits");
    }
    else(printf("Special Charaters"));

    return 0;
}
