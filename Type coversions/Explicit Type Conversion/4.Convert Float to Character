Take a float, convert it to an integer, then print the corresponding ASCII character.

#include <stdio.h>

int main()
{
    float a;
    scanf("%f",&a);
    int b=a;
    printf("%c",b);
    
    return 0;
}
